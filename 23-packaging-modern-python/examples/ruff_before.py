"""Deliberately messy code -- unused imports, unsorted imports, bad
spacing, an unused variable. Run `ruff check ruff_before.py` and
`ruff format --diff ruff_before.py` against this file to see Ruff catch
every one of these (see README.md for the exact captured output).
"""
import os
import sys
from collections import OrderedDict


def messy(  a,b ):
    x=1
    return a+b
